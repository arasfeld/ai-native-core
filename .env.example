# Optional: use OpenAI (set API key) or Ollama (set base URL and optionally model).
# For Ollama: copy to .env and set OPENAI_BASE_URL. No API key needed.
# For OpenAI: set OPENAI_API_KEY (and optionally OPENAI_MODEL).

# OPENAI_BASE_URL=http://localhost:11434/v1
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=llama3.2
