{
  "name": "desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tauri build",
    "clean": "rm -rf dist node_modules",
    "dev": "tauri dev",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  }
}
