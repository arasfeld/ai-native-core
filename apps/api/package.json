{
  "name": "api",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist node_modules __pycache__ .pytest_cache .ruff_cache",
    "dev": "uv run api-dev",
    "format": "uv run ruff format .",
    "lint": "uv run ruff check .",
    "test": "uv run pytest"
  }
}
